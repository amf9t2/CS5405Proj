package code;

import java.awt.*;
import java.awt.event.*;
import java.util.Date;

/**
 @author Altamash M Fakki
 @version 2.0
 */
/** HeapSort class extends Sort*/
//References
//1.http://en.wikipedia.org/wiki/Heapsort

public class HeapSort extends Sort
{
    public HeapSort()
    {
        super("Heap", "Heap sort, Heapsort can be thought of as an improved selection sort: like that algorithm, it divides its input into a sorted and an unsorted region, and it iteratively shrinks the unsorted region by extracting the smallest element and moving that to the sorted region. The improvement consists of the use of a heap data structure rather than a linear-time search to find the minimum.", 0,  0, 0);
    }
    
    public void algorithm(){
        
    }
}
